// SPDX-FileCopyrightText: 2026 OpenDX CompanyOS contributors
// SPDX-License-Identifier: Apache-2.0
import { randomUUID } from "node:crypto"; import { Pool } from "pg"; import request from "supertest"; import { afterAll,beforeAll,describe,expect,it } from "vitest"; import { createApiApp } from "../../../app"; import { createPostgresPool } from "../../../shared/database/postgres"; import { PostgresTransactionRunner } from "../../../shared/database/transaction"; import { runCatalogMigrations } from "../../../shared/database/run-migrations"; import type { GoogleIdentityVerifier } from "../application/identity/google-identity-verifier"; import { createCustomerModule } from "../customer.module"; import { runCustomerMigrations } from "../infrastructure/database/run-customer-migrations"; import { NodeSessionTokenService } from "../infrastructure/security/node-session-token-service";
const databaseUrl=process.env.TEST_DATABASE_URL;const suite=databaseUrl===undefined?describe.skip:describe;const origin="http://localhost:3100";
suite("customer API integration",()=>{const pool=new Pool({connectionString:databaseUrl});let app:ReturnType<typeof createApiApp>;beforeAll(async()=>{await runCatalogMigrations(databaseUrl!,"up");await runCustomerMigrations(databaseUrl!,"up");const verifier:GoogleIdentityVerifier={async verify(){return{provider:"google",subject:"subject-1",email:"customer@example.com",emailVerified:true,verifiedAt:"2026-08-05T00:00:00.000Z"};}};const module=createCustomerModule({transactions:new PostgresTransactionRunner(createPostgresPool({databaseUrl:databaseUrl!})),verifier,tokens:new NodeSessionTokenService(),generateId:randomUUID,now:()=>new Date().toISOString(),storefrontOrigin:origin,cookies:{guestName:"opendx_guest",customerName:"opendx_customer",csrfName:"opendx_csrf",secure:false},authenticationRateLimit:20});app=createApiApp({storefrontOrigin:origin,storefrontRouter:module.router});});afterAll(async()=>{await runCustomerMigrations(databaseUrl!,"down");await runCatalogMigrations(databaseUrl!,"down");await pool.end();});
 it("creates guest and rotated customer sessions without exposing raw tokens",async()=>{const agent=request.agent(app);const guest=await agent.post("/v1/storefront/guest-sessions").set("Origin",origin).expect(201);expect((guest.headers["set-cookie"] as unknown as string[]).join(";")).toContain("opendx_guest=");const login=await agent.post("/v1/storefront/auth/google").set("Origin",origin).send({credential:"signed-google-credential"}).expect(200);expect(login.body.data).toMatchObject({kind:"customer",email:"customer@example.com"});expect(JSON.stringify(login.body)).not.toContain("subject-1");const restored=await agent.get("/v1/storefront/session").expect(200);expect(restored.body.data.kind).toBe("customer");const stored=await pool.query("SELECT token_hash FROM customer_sessions");expect(stored.rows[0].token_hash).toMatch(/^[a-f0-9]{64}$/);});
 it("enforces origin and CSRF before owned address mutations",async()=>{const agent=request.agent(app);const login=await agent.post("/v1/storefront/auth/google").set("Origin",origin).send({credential:"signed-google-credential"}).expect(200);await agent.post("/v1/storefront/account/addresses").set("Origin",origin).send({recipientName:"Duy",phoneNumber:"0900000000",addressLine:"1 Nguyen Hue",ward:"Ben Nghe",provinceOrCity:"Ho Chi Minh City"}).expect(403);const csrfCookie=(login.headers["set-cookie"] as unknown as string[]).find((v)=>v.startsWith("opendx_csrf="))!;const csrf=csrfCookie.split(";",1)[0]!.split("=",2)[1]!;const created=await agent.post("/v1/storefront/account/addresses").set("Origin",origin).set("x-csrf-token",csrf).send({recipientName:"Duy",phoneNumber:"0900000000",addressLine:"1 Nguyen Hue",ward:"Ben Nghe",provinceOrCity:"Ho Chi Minh City"}).expect(201);expect(created.body.data.isDefault).toBe(true);await agent.post("/v1/storefront/account/addresses").set("Origin","http://evil.example").set("x-csrf-token",csrf).send({}).expect(403);});
});
