// SPDX-FileCopyrightText: 2026 OpenDX CompanyOS contributors
// SPDX-License-Identifier: Apache-2.0
import { timingSafeEqual } from "node:crypto"; import type { RequestHandler } from "express"; import { ApplicationError } from "../../../../shared/http/application-error"; import { readCookie,type StorefrontCookieConfig } from "./storefront-cookies";
export function requireStorefrontOrigin(origin:string):RequestHandler{return(req,_res,next)=>{if(req.header("origin")!==origin){next(new ApplicationError(403,"CSRF_INVALID","Storefront origin is invalid"));return;}next();};}
export function requireCsrf(config:StorefrontCookieConfig):RequestHandler{return(req,_res,next)=>{const cookie=readCookie(req,config.csrfName),header=req.header("x-csrf-token");if(cookie===undefined||header===undefined){next(new ApplicationError(403,"CSRF_INVALID","CSRF token is invalid"));return;}const a=Buffer.from(cookie),b=Buffer.from(header);if(a.length!==b.length||!timingSafeEqual(a,b)){next(new ApplicationError(403,"CSRF_INVALID","CSRF token is invalid"));return;}next();};}
