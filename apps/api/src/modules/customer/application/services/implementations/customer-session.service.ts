// SPDX-FileCopyrightText: 2026 OpenDX CompanyOS contributors
// SPDX-License-Identifier: Apache-2.0
import type { TransactionRunner } from "../../../../../shared/database/transaction"; import type { CustomerRepository } from "../../repositories/interfaces/customer.repository"; import type { SessionTokenService } from "../../security/session-token-service"; import type { CustomerSessionServiceContract } from "../interfaces/customer-session.service"; import { CustomerApplicationError } from "../customer-application.error"; import { GUEST_SESSION_TTL_MS, assertCustomerSessionActive, assertGuestSessionActive, rotateCustomerSession, sessionExpiresAt } from "../../../domain/services/customer-rules";
export class CustomerSessionService implements CustomerSessionServiceContract { constructor(private readonly repo:CustomerRepository,private readonly tx:TransactionRunner,private readonly tokens:SessionTokenService,private readonly id:()=>string,private readonly now:()=>string){}
 async createGuest(){const at=this.now(),token=this.tokens.generate(),session={id:this.id(),tokenHash:token.hash,expiresAt:sessionExpiresAt(at,GUEST_SESSION_TTL_MS),lastSeenAt:at,createdAt:at};await this.tx.run(s=>this.repo.createGuestSession(s,session));return{rawToken:token.raw,principal:{guestSessionId:session.id,expiresAt:session.expiresAt}};}
 async resolveGuest(raw:string){return this.tx.runReadOnly(async s=>{const x=await this.repo.findGuestSessionByHash(s,this.tokens.hash(raw));if(x===undefined)throw new CustomerApplicationError("CUSTOMER_SESSION_EXPIRED","Guest session is invalid");try{assertGuestSessionActive(x,this.now());}catch{throw new CustomerApplicationError("CUSTOMER_SESSION_EXPIRED","Guest session is expired");}return{guestSessionId:x.id,expiresAt:x.expiresAt};});}
 async resolveCustomer(raw:string,rotate=true){const hash=this.tokens.hash(raw),at=this.now();return this.tx.run(async s=>{const x=await this.repo.findCustomerSessionByHash(s,hash,true);if(x===undefined)throw new CustomerApplicationError("CUSTOMER_SESSION_EXPIRED","Customer session is invalid");try{assertCustomerSessionActive(x,at);}catch{throw new CustomerApplicationError("CUSTOMER_SESSION_EXPIRED","Customer session is expired");}const customer=await this.repo.findCustomerById(s,x.customerId);if(customer===undefined||customer.status!=="active")throw new CustomerApplicationError("CUSTOMER_DISABLED","Customer is disabled");if(!rotate)return{rawToken:raw,principal:{customerId:customer.id,sessionId:x.id,email:customer.email,expiresAt:x.expiresAt}};const token=this.tokens.generate(),next=rotateCustomerSession(x,token.hash,at);await this.repo.replaceCustomerSession(s,next);return{rawToken:token.raw,principal:{customerId:customer.id,sessionId:x.id,email:customer.email,expiresAt:x.expiresAt}};});}
}
