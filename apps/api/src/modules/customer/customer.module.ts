// SPDX-FileCopyrightText: 2026 OpenDX CompanyOS contributors
// SPDX-License-Identifier: Apache-2.0
import { Router } from "express"; import type { TransactionRunner } from "../../shared/database/transaction"; import type { GoogleIdentityVerifier } from "./application/identity/google-identity-verifier"; import type { SessionTokenService } from "./application/security/session-token-service"; import { CustomerAuthenticationService } from "./application/services/implementations/customer-authentication.service"; import { CustomerSessionService } from "./application/services/implementations/customer-session.service"; import { CustomerProfileService } from "./application/services/implementations/customer-profile.service"; import { PostgresqlCustomerRepository } from "./infrastructure/repositories/implementations/postgresql-customer.repository"; import { PostgresqlCustomerAuditRepository } from "./infrastructure/repositories/implementations/postgresql-customer-audit.repository"; import { CustomerAuthController } from "./presentation/controllers/customer-auth.controller"; import { CustomerAccountController } from "./presentation/controllers/customer-account.controller"; import { createCustomerAuthRouter } from "./presentation/routes/customer-auth.routes"; import { createCustomerAccountRouter } from "./presentation/routes/customer-account.routes"; import { requireCustomerSession } from "./presentation/middleware/customer-session.middleware"; import { requireCsrf,requireStorefrontOrigin } from "./presentation/middleware/storefront-mutation.middleware"; import { createAuthenticationRateLimit } from "./presentation/middleware/storefront-rate-limit.middleware"; import type { StorefrontCookieConfig } from "./presentation/middleware/storefront-cookies"; import { customerErrorMiddleware } from "./presentation/middleware/customer-error.middleware";
export interface CustomerModuleDependencies {readonly transactions:TransactionRunner;readonly verifier:GoogleIdentityVerifier;readonly tokens:SessionTokenService;readonly generateId:()=>string;readonly now:()=>string;readonly storefrontOrigin:string;readonly cookies:StorefrontCookieConfig;readonly authenticationRateLimit:number}
export function createCustomerModule(d:CustomerModuleDependencies){const repo=new PostgresqlCustomerRepository(),audit=new PostgresqlCustomerAuditRepository(),sessions=new CustomerSessionService(repo,d.transactions,d.tokens,d.generateId,d.now),auth=new CustomerAuthenticationService(repo,audit,d.transactions,d.verifier,d.tokens,d.generateId,d.now),profile=new CustomerProfileService(repo,d.transactions,d.generateId,d.now),origin=requireStorefrontOrigin(d.storefrontOrigin),csrf=requireCsrf(d.cookies),customer=requireCustomerSession(sessions,d.cookies),router=Router();router.use(createCustomerAuthRouter(new CustomerAuthController(auth,sessions,d.cookies),origin,csrf,createAuthenticationRateLimit(d.authenticationRateLimit)));router.use(createCustomerAccountRouter(new CustomerAccountController(profile),customer,origin,csrf));router.use(customerErrorMiddleware);return{router,sessions,profile};}
