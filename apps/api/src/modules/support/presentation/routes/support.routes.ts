// SPDX-FileCopyrightText: 2026 OpenDX CompanyOS contributors
// SPDX-License-Identifier: Apache-2.0
import {Router,type RequestHandler} from "express";import {createAuditedRoleGuard,type DeniedAuditContext} from "../../../../shared/auth/audited-role-guard.middleware";import type {SupportController} from "../controllers/support.controller";export function createSupportRouter(c:SupportController,a:RequestHandler,denied:(x:DeniedAuditContext)=>Promise<void>){const r=Router();const guard=createAuditedRoleGuard({allowedRoles:["administrator","crm_operator","support_operator"],action:"support.access.denied",resourceId:q=>typeof q.params.ticketId==="string"?q.params.ticketId:"tickets",appendDenied:denied});r.use(a,guard);r.get("/",c.list);r.post("/",c.create);r.get("/:ticketId",c.detail);r.post("/:ticketId/claim",c.claim);r.patch("/:ticketId",c.transition);r.post("/:ticketId/messages",c.message);return r;}
