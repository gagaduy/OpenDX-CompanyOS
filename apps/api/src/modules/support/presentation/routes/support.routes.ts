// SPDX-FileCopyrightText: 2026 OpenDX CompanyOS contributors
// SPDX-License-Identifier: Apache-2.0
import {Router,type RequestHandler} from "express";import multer,{MulterError} from "multer";import {ApplicationError} from "../../../../shared/http/application-error";import {createAuditedRoleGuard,type DeniedAuditContext} from "../../../../shared/auth/audited-role-guard.middleware";import type {SupportController} from "../controllers/support.controller";export function createSupportRouter(c:SupportController,a:RequestHandler,denied:(x:DeniedAuditContext)=>Promise<void>,maximumAttachmentBytes=25*1024*1024){const r=Router();const guard=createAuditedRoleGuard({allowedRoles:["administrator","crm_operator","support_operator"],action:"support.access.denied",resourceId:q=>typeof q.params.ticketId==="string"?q.params.ticketId:"tickets",appendDenied:denied});const parser=multer({storage:multer.memoryStorage(),limits:{fileSize:maximumAttachmentBytes,files:1}}).single("file");const parseUpload:RequestHandler=(q,s,n)=>{parser(q,s,e=>{if(e instanceof MulterError&&e.code==="LIMIT_FILE_SIZE"){n(new ApplicationError(413,"ATTACHMENT_TOO_LARGE","Attachment exceeds the upload limit"));return;}n(e);});};r.use(a,guard);r.get("/",c.list);r.post("/",c.create);r.get("/:ticketId",c.detail);r.post("/:ticketId/claim",c.claim);r.patch("/:ticketId",c.transition);r.post("/:ticketId/messages",c.message);r.post("/:ticketId/attachments",parseUpload,c.uploadAttachment);r.get("/:ticketId/attachments/:attachmentId/content",c.downloadAttachment);return r;}
