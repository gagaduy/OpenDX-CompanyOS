// SPDX-FileCopyrightText: 2026 OpenDX CompanyOS contributors
// SPDX-License-Identifier: Apache-2.0

import { useState, type FormEvent } from "react";
import type { AgenticTaskIntake } from "../types/agentic.types";
export function TaskIntakeForm({ onSubmit, submitting, message }: { readonly onSubmit: (input: AgenticTaskIntake) => void; readonly submitting: boolean; readonly message?: string }) { const [mode, setMode] = useState<"store_health_review" | "advanced">("store_health_review"); const submit = (event: FormEvent<HTMLFormElement>) => { event.preventDefault(); const form = new FormData(event.currentTarget); const reviewWindow = { start: String(form.get("reviewStart") ?? ""), end: String(form.get("reviewEnd") ?? "") }; onSubmit({ mode, goal: String(form.get("goal") ?? ""), instructions: String(form.get("instructions") ?? ""), ...(mode === "store_health_review" ? { reviewWindow } : {}) }); }; return <form className="agenticIntakeForm" onSubmit={submit}><fieldset><legend>Task mode</legend><label><input type="radio" checked={mode === "store_health_review"} onChange={() => setMode("store_health_review")} />Store Health review</label><label><input type="radio" checked={mode === "advanced"} onChange={() => setMode("advanced")} />Advanced</label></fieldset><label>Goal<input name="goal" required /></label><label>Instructions<textarea name="instructions" required /></label>{mode === "store_health_review" && <div className="agenticReviewWindow"><label>Review start<input aria-label="Review start" name="reviewStart" type="date" required /></label><label>Review end<input aria-label="Review end" name="reviewEnd" type="date" required /></label></div>}<button className="primaryButton" type="submit" disabled={submitting}>{submitting ? "Creating…" : "Create task"}</button>{message && <p role="status">{message}</p>}</form>; }
